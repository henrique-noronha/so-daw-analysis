% ----------------------------------------------------------------------------------------------------- %
% Arquivo Principal - Trabalho de Sistemas Operacionais
% ----------------------------------------------------------------------------------------------------- %

\documentclass[tcc1]{classe_uftex/uftex}
\newcommand\uftex{UF\TeX}

\usepackage{lipsum}
\usepackage{tikz}
\usepackage[siunitx]{circuitikz}
\usetikzlibrary{arrows}
\usepackage{multirow}
\usepackage{graphicx}
\graphicspath{{images/}}
\usepackage{booktabs}

\usepackage[alf,abnt-emphasize=bf]{referencias_abnt/abntex2cite}
\renewcommand{\backrefpagesname}{}
\renewcommand{\backref}{}
\renewcommand*{\backrefalt}[4]{}

\makelosymbols
\makeloabbreviations

% ========================================================================= %
% PATCH DE CORREÇÃO
% ========================================================================= %
\makeatletter
\renewcommand\maketitle{%
  \ltx@ifpackageloaded{hyperref}{\uftex@hypersetup}{}%
 \begin{titlepage}
 \begin{center}
 \includegraphics[scale=.3]{classe_uftex/logouft}\\
 \setstretch{1}
 \MakeUppercase{\bf\frontcover@maintext}
 \end{center}
  \begin{center}
  \vspace{4cm}
  \nohyphens{\MakeUppercase{\bf Henrique Noronha}}\par
  \nohyphens{\MakeUppercase{\bf Lauro Oliveira Mota}}\par
  \nohyphens{\MakeUppercase{\bf João Victor Melo do Nascimento}}\par
  \nohyphens{\MakeUppercase{\bf Hátilan Caio Alves Fontes}}\par
  \vspace{4cm}
  \nohyphens{\bf\MakeUppercase\local@title}\par
  \vfill
  {\bf\frontpage@bottomtext}
  \end{center}
  \end{titlepage}
  \global\let\maketitle\relax \global\let\and\relax
}

\renewcommand\makefrontpage{%
  \begin{center}
  {\bf Henrique Noronha}\par
  {\bf Lauro Oliveira Mota}\par
  {\bf João Victor Melo do Nascimento}\par
  {\bf Hátilan Caio Alves Fontes}\par
  \vfill
   \nohyphens{\bf \local@title}\par
  \end{center}
  \vfill
  \begin{flushright}
  \begin{minipage}{8.45cm}
  \setstretch{1}
  \frontpage@maintext
  \end{minipage}\par
 \end{flushright}
  \vfill
  \frontpage@bottomtext}

\renewcommand\frontpage@maintext{
  \sloppy\nohyphens{\local@doctype\ apresentado à \local@universityname\ para obten{\c c}{\~ a}o do t{\' i}tulo de \local@degname\ em \local@deptname .}
}

\renewenvironment{abstract}{%
\cleardoublepage \thispagestyle{empty} \chapter*{Resumo} \setlength{\parindent}{0pt}
}{\vspace{.5cm}
\count1=0
\noindent\nohyphens{{\bfseries Palavra-chave}:} \@whilenum \count1<\value{keywords} \do {%
        \nohyphens{\csname uftKeyword:\the\count1 \endcsname.}
        \advance\count1 by 1}\par}

\renewenvironment{foreignabstract}{%
\cleardoublepage \thispagestyle{empty} \chapter*{Abstract} \setlength{\parindent}{0pt}
}{\vspace{.5cm}
\noindent\nohyphens{{\bfseries Keywords}:}
\count1=0
    \@whilenum \count1<\@foreignkeyword \do{%
    \nohyphens{\protect\csname uftForeignkeyword:\the\count1\expandafter\endcsname .}
    \advance\count1 by 1}\par}

\def\@degreename{Bacharel}
\def\local@degname{Bacharel}
\def\local@deptname{Ciência da Computação}
\def\local@programname{Ciência da Computação}
\def\local@doctype{Relatório de Sistemas Operacionais}
\makeatother
% ========================================================================= %

\begin{document}

  \title{Análise Comparativa de Gerenciamento de Recursos do SO: Ableton Live vs. FL Studio}
  \author{Henrique}{Noronha Fernandes}
 
  \advisor{Prof.}{Antonio Marcos A.}{Ferreira}{Dr.}

  \department{Ciência da Computação}
  \date{15}{04}{2026}

  \keyword{Sistemas Operacionais}
  \keyword{Escalonamento de Threads}
  \keyword{Gerenciamento de Memória}
  \keyword{Latência de Áudio}
  \keyword{Análise de Desempenho}

  \foreignkeyword{Operating Systems}
  \foreignkeyword{Thread Scheduling}
  \foreignkeyword{Memory Management}
  \foreignkeyword{Audio Latency}
  \foreignkeyword{Performance Analysis}

  \maketitle
  \frontmatter

  \printlosymbols
  \printloabbreviations
  \tableofcontents

\mainmatter
\setstretch{1.0}

% ----------------------------------------------------------------------------------------------------- %
\chapter{Introdução}

Os Sistemas Operacionais (SO) modernos precisam lidar com arquiteturas
de múltiplos núcleos, memória virtual e interrupções em escala de
milissegundos. Nesse cenário, muitos computadores pessoais acabam sendo
usados como Estações de Trabalho de Áudio Digital (DAWs, do inglês
\textit{Digital Audio Workstations}), cuja performance não depende só
da força bruta do processador, mas também de como o SO gerencia as
\textit{threads}, aloca memória e cuida das operações de I/O. O grande
desafio desse tipo de aplicação é operar sob restrições de tempo real.
Segundo \citeonline{silberschatz2018}, as DAWs se enquadram como
sistemas de tempo real não crítico (\textit{soft real-time}): um atraso
no atendimento não causa falha grave, mas se manifesta como falhas e
artefatos sonoros. Para evitar isso, o SO precisa garantir que as
\textit{threads} de áudio tenham preferência sobre os processos comuns.

A partir disso, este trabalho faz uma análise comparativa do
gerenciamento de recursos do SO entre duas das DAWs mais usadas no
mercado: o Ableton~Live e o FL~Studio. A ideia é mapear como cada
arquitetura afeta o consumo de CPU, o comportamento \textit{multithread},
o uso de memória RAM e a latência de I/O sob as mesmas condições de
estresse.

O restante do trabalho está organizado em cinco capítulos. O Capítulo~2
apresenta o referencial teórico sobre processos, \textit{threads},
escalonamento, interrupções, latência de I/O e gerenciamento de
memória, com foco no Windows NT. O Capítulo~3 descreve a metodologia,
as ferramentas usadas (PerfMon, Process Explorer e LatencyMon) e o
protocolo dos cinco testes. O Capítulo~4 reúne os resultados de cada
teste, e o Capítulo~5 discute as diferenças arquiteturais observadas
entre as duas DAWs. Por fim, o Capítulo~6 sintetiza as principais
conclusões do estudo.


% ----------------------------------------------------------------------------------------------------- %
\chapter{Referencial Teórico}

\section{Processos, \textit{threads} e escalonamento}

Um processo é um programa em execução, com espaço de endereçamento
próprio e os recursos a ele atribuídos pelo SO \cite{silberschatz2018}.
Dentro dele podem coexistir várias \textit{threads}, que compartilham
esse espaço mas mantêm pilha e registradores próprios
\cite{tanenbaum2016}. Como a troca de contexto entre \textit{threads}
é mais barata do que entre processos, motores de áudio recorrem ao
\textit{multithreading} para distribuir o processamento entre os
núcleos do hardware. O escalonador do Windows NT é preemptivo baseado
em prioridades, com 32 níveis: a faixa de usuário ocupa 1--15 e a
faixa de tempo real, 16--31 \cite{stallings2017os}. Os valores
observados aqui ficam todos na faixa de usuário: \textit{Normal} (8),
\textit{Above Normal} (10) e \textit{High} (12--13). As DAWs se
enquadram como sistemas \textit{soft real-time}: um \textit{buffer}
não preenchido a tempo gera \textit{dropout} audível, mas o sistema
continua estável \cite{silberschatz2018}.

\section{Interrupções, latência e drivers de áudio}

Uma interrupção de hardware é um sinal assíncrono pelo qual um
periférico avisa o processador de que precisa de atenção imediata
\cite{silberschatz2018}. A soma da latência da Rotina de Serviço de
Interrupção (ISR) com a troca de contexto até a \textit{thread} destino
é a latência de despacho \cite{stallings2017os}. Os drivers de áudio
operam no modelo \textit{pull callback}: o hardware pede periodicamente
um bloco de $N$ amostras e o motor tem até o prazo do próximo bloco
para entregá-lo \cite{fons2010}. A relação entre \textit{buffer} e
latência é direta, $L = N / f_s$, onde $f_s$ é a taxa de amostragem;
reduzir $N$ diminui a latência mas aumenta a frequência de interrupções
\cite{kim2003}. No Windows, o caminho WDM/DirectSound passa pelo
\textit{Kernel Audio Mixer} (KMixer), que mistura fluxos de vários
aplicativos e precisa ser acordado a cada ciclo de \textit{buffer}; já
o protocolo ASIO dá ao driver do fabricante acesso direto ao hardware,
ignorando o KMixer \cite{microsoft2023asio}.

\section{Memória, paginação e estratégias de alocação}

Cada processo recebe do SO um espaço de endereçamento virtual contíguo
e isolado \cite{silberschatz2018}, com paginação por demanda: uma
página só é mapeada a um quadro físico quando é acessada pela primeira
vez (\textit{page fault}) \cite{tanenbaum2016}. Para áudio em tempo
real, uma falta no meio do processamento de um bloco é cara, porque a
\textit{thread} fica bloqueada enquanto o prazo do \textit{buffer}
continua correndo \cite{kim2003}. Por isso, DAWs comerciais costumam
usar \textit{eager allocation} (que reserva memória logo na
inicialização e elimina faltas no caminho crítico) em vez de
\textit{lazy allocation} (que mantém pegada mínima mas introduz
latência na primeira alocação). O contador \textit{Private Bytes} do
Windows, usado neste trabalho, mede a memória virtual comprometida só
pelo processo, sem contar DLLs e regiões compartilhadas
\cite{stallings2018}.


% ----------------------------------------------------------------------------------------------------- %
\chapter{Metodologia}

O estudo adota uma abordagem quantitativa e comparativa para analisar o
gerenciamento de recursos de \textit{Digital Audio Workstations} (DAWs)
sob a ótica de Sistemas Operacionais. O foco é a interação entre o
\textit{kernel} do Windows~11 (build~26200, x64) e os motores de áudio do
Ableton~Live~12 e do FL~Studio~24. Os experimentos foram feitos em um
notebook ASUS Vivobook K3605ZF com processador Intel Core i5-12500H
(12~núcleos, 16~\textit{threads}, base 3,1~GHz), 16~GB de RAM DDR4 e GPU
NVIDIA (driver \texttt{nvlddmkm.sys} v581.29). Para preservar as escolhas
arquiteturais nativas de cada fabricante, o FL~Studio rodou com o driver
FL~Studio ASIO e o Ableton~Live rodou com MME/DirectX. O \textit{Fruity
Limiter} do canal Master do FL~Studio foi removido antes dos testes para
manter paridade na cadeia de sinal. O Windows Defender ficou ativo em
todas as sessões, em condições iguais para as duas DAWs.

A coleta usou três ferramentas: o Performance Monitor (PerfMon) para
capturar contadores do \textit{kernel} a 1~Hz
(\texttt{\% Tempo de Processador}, \texttt{Private Bytes} e
\texttt{Contagem de Threads}), exportados via \texttt{relog} para
\texttt{.csv} \cite{microsoft2023perfmon}; o Process Explorer da
Sysinternals para inspecionar \textit{threads} individuais, prioridades
e consumo de CPU; e o LatencyMon v7.31 \cite{asio4all} para medir
latência \textit{interrupt-to-process}, tempos de DPC/ISR e
\textit{hard pagefaults}.

O protocolo experimental foi composto por cinco testes sequenciais. O
Teste~1, de repouso, coletou dados durante 2,5~minutos com a DAW aberta
e sem projeto carregado. O Teste~2, de carga \textit{I/O-bound},
adicionou a reprodução simultânea em \textit{loop} de 20~trilhas AIFF
(PCM não comprimido) por 3~minutos. No Teste~3, de escalonamento, o
Process Explorer foi usado para inspecionar a composição, a prioridade e
a distribuição das \textit{threads} com o projeto ativo. No Teste~4, de
latência, o LatencyMon foi executado por 2~min~50~s sobre o mesmo
projeto, registrando DPCs, ISRs e \textit{pagefaults}. Por fim, no
Teste~5, de estresse CPU-bound, foram inseridas cinco instâncias do
reverb nativo de cada DAW no canal Master, e o tamanho do \textit{buffer}
foi reduzido aos poucos de 4096 até 128~\textit{samples}. O ponto de
ruptura (\textit{buffer underrun}) foi identificado pelo aparecimento de
\textit{dropouts} audíveis.

O trabalho se caracteriza como um estudo de caso exploratório, sem
pretensão de generalização estatística. Para extrair intervalos de
confiança a partir das séries de 145 a 178 amostras coletadas por sessão,
foi aplicada a técnica de \textit{bootstrap} com reamostragem por
reposição proposta por \citeonline{efron1979}, gerando $B = 10\,000$
reamostragens por métrica, valor recomendado para estimativas estáveis
de intervalos percentílicos em $B \geq 1\,000$, com $10\,000$ como
escolha conservadora usual em estudos empíricos. Os intervalos de
confiança de 95\% são reportados ao lado das médias.


% ----------------------------------------------------------------------------------------------------- %
\chapter{Resultados}

\section{Testes 1 e 2: consumo em repouso e carga I/O-bound}

Os Testes~1 e~2 avaliaram o comportamento de CPU, memória e
\textit{threads} em dois cenários consecutivos: DAW em repouso (sem projeto)
e com reprodução simultânea de 20~trilhas AIFF em \textit{loop}. Os dados,
amostrados a 1~Hz via PerfMon durante 2,5 e 3~minutos respectivamente, estão
consolidados nas Tabelas~\ref{tab:t1} e~\ref{tab:t2}.

\begin{table}[ht]
\centering
\small
\caption{\small Médias dos Testes~1 (repouso) e~2 (20 trilhas AIFF). Valores entre parênteses indicam o máximo observado.}
\label{tab:t1}
\begin{tabular}{lcccc}
\toprule
\textbf{Métrica} & \textbf{T1 Ableton} & \textbf{T1 FL Studio} & \textbf{T2 Ableton} & \textbf{T2 FL Studio} \\
\midrule
\% CPU média (máx)         & 10{,}95 (33{,}73) & 12{,}38 (37{,}72) & 3{,}74 (20{,}11) & 4{,}80 (31{,}16) \\
\textit{Private Bytes} (MB) & 536{,}8 & 95{,}8 & 601{,}7 & 147{,}6 \\
\textit{Threads} ativas    & 54{,}8 & 44{,}0 & 55{,}3 & 27{,}0 \\
\bottomrule
\end{tabular}
\end{table}

Em repouso, o FL~Studio consumiu CPU média um pouco maior
($12{,}38\%$ contra $10{,}95\%$), mas o Ableton~Live alocou $5{,}6$
vezes mais memória ($536{,}8$~MB contra $95{,}8$~MB) e quase o dobro de
\textit{threads} ($54{,}8$ contra $44{,}0$). Com as 20~trilhas tocando,
as duas DAWs reduziram a média de CPU (o Ableton para $3{,}74\%$ e o
FL~Studio para $4{,}80\%$), a memória cresceu pouco ($+65$ e $+52$~MB)
e o FL~Studio reduziu suas \textit{threads} de 44 para 27 enquanto o
Ableton ficou praticamente igual. A Figura~\ref{fig:t1cpu} mostra a
evolução da CPU nos dois cenários.

\begin{figure}[ht]
\centering
\includegraphics[width=0.48\textwidth]{grafico_cpu}\hfill
\includegraphics[width=0.48\textwidth]{t2_grafico_cpu}
\caption{\small Consumo de CPU em repouso (esq.) e durante a reprodução de 20 trilhas AIFF (dir.).}
\label{fig:t1cpu}
\end{figure}

% ---------------------------------------------------------------- %
\section{Teste 3: escalonamento de threads}

A inspeção via Process Explorer com o projeto em reprodução mostrou
diferenças na composição e na prioridade das \textit{threads}
(Tabela~\ref{tab:t3}). O FL~Studio roda com prioridade base~10
(\textit{Above Normal}) e dinâmica~12 (\textit{High}), enquanto o
Ableton~Live fica em base~8 (\textit{Normal}) e dinâmica~10. Nenhuma
das duas atinge o nível \textit{Time Critical} (15). Na composição, 14
das 29~\textit{threads} do FL~Studio pertencem ao \texttt{QuickFontCache}
(renderização da interface) e só 5 ao \texttt{FLEngine\_x64} (motor de
áudio). No Ableton~Live, cerca de 30 das 55~\textit{threads} vêm do
\texttt{ucrtbase.dll}, que é o \textit{thread pool} do C runtime.

\begin{table}[ht]
\centering
\small
\caption{\small Composição e prioridade das \textit{threads} no Teste 3, obtidas via Process Explorer.}
\label{tab:t3}
\begin{tabular}{lcc}
\toprule
\textbf{Parâmetro} & \textbf{FL Studio (FL64)} & \textbf{Ableton Live 12} \\
\midrule
Total de \textit{threads}           & 29                & 55                \\
Prioridade base (thread principal)  & 10 (Above Normal) & 8 (Normal)        \\
Prioridade dinâmica                 & 12 (High)         & 10 (Above Normal) \\
CPU da thread principal             & 0{,}93\%          & 0{,}75\%          \\
Thread \textit{Time Critical} (15)? & Não               & Não               \\
Módulo dominante                    & QuickFontCache    & ucrtbase          \\
Thread de driver de áudio           & ilwasapi2asio\_x64& DSOUND.dll        \\
\bottomrule
\end{tabular}
\end{table}

\section{Teste 4: latência com driver nativo}

O LatencyMon classificou as duas DAWs como aptas, com latência máxima
\textit{interrupt-to-process} de $805{,}40\,\mu$s no Ableton~Live e
$939{,}80\,\mu$s no FL~Studio, ambas abaixo do limite de $1000\,\mu$s
adotado pela ferramenta como referência para áudio em tempo real
\cite{asio4all}. O
maior DPC do FL~Studio veio do driver NVIDIA \texttt{nvlddmkm.sys}
($647\,\mu$s) e o do Ableton~Live veio do \texttt{tcpip.sys}
($449\,\mu$s). Em ambas as sessões, o processo com mais \textit{hard
pagefaults} foi o \texttt{msmpeng.exe} do Windows Defender (306 no
Ableton e 378 no FL~Studio).

% ---------------------------------------------------------------- %
\section{Teste 5: estresse CPU-bound e ponto de ruptura}

Com cinco instâncias de reverb no canal Master e as 20~trilhas em
reprodução, reduzimos o \textit{buffer} aos poucos até começar a aparecer
\textit{dropouts} audíveis. O FL~Studio com ASIO se manteve estável até
$256$~\textit{samples} ($5{,}8$~ms) e quebrou em $128$~\textit{samples}
($2{,}9$~ms). Já o Ableton~Live com MME/DirectX começou a falhar em
$1024$~\textit{samples} ($23{,}2$~ms), com degradação crescente nos
valores menores. A Figura~\ref{fig:t5lat} ilustra esse comportamento.

\begin{figure}[ht]
\centering
\includegraphics[width=0.65\textwidth]{t5_buffer_latencia}
\caption{\small Tamanho do \textit{buffer} e latência correspondente no Teste 5. O marcador $\times$ indica o ponto de \textit{dropout} em cada DAW.}
\label{fig:t5lat}
\end{figure}

% ----------------------------------------------------------------------------------------------------- %

\chapter{Discussão}

\section{Padrões arquiteturais (T1-T3)}

Os Testes~1 a~3 deixam claro que as duas DAWs seguem caminhos opostos
desde a inicialização. O Ableton~Live usa \textit{eager allocation}, com
537~MB e 55~\textit{threads} já em repouso, o que mantém a latência de
despacho baixa desde o início \cite{silberschatz2018}. O FL~Studio adota
\textit{lazy allocation}, com 96~MB e 44~\textit{threads} em repouso, e
só amplia esses recursos quando o projeto é carregado \cite{tanenbaum2016}.
A queda de \textit{threads} do FL~Studio (de 44 para 27) ao carregar o
projeto sugere uma consolidação de \textit{threads} genéricas em outras
mais especializadas para áudio. No escalonamento, o FL~Studio compensa o
menor número de \textit{threads} com prioridade mais alta (base~10,
dinâmica~12), mas boa parte desse orçamento é consumido pelo
\texttt{QuickFontCache} da interface, e não pelo motor de áudio. O
Ableton~Live, mesmo em prioridade \textit{Normal} (8), distribui a carga
em 55~\textit{threads} via \textit{thread pool} do \texttt{ucrtbase}, o
que favorece a escalabilidade nos 16~\textit{threads} lógicos do
i5-12500H. Nenhuma das duas usa prioridade \textit{Time Critical} (15),
o que confirma o enquadramento delas como \textit{soft real-time}
\cite{stallings2017os}.

\section{Comportamento sob carga e latência (T4-T5)}

Em ambiente controlado, o LatencyMon classificou as duas DAWs como
aptas, com latências máximas abaixo do limite de $1000\,\mu$s. O maior
DPC do FL~Studio veio do driver NVIDIA (\texttt{nvlddmkm.sys}), o que
indica que a GPU gera interrupções longas e disputa o escalonador com o
motor de áudio, independentemente da DAW. Em uma sessão anterior, com o
Windows Defender em surto de varredura, a latência do FL~Studio chegou
a $5274\,\mu$s e o sistema foi classificado como não apto, o que mostra
que interferências externas podem ter impacto até maior do que as
próprias diferenças entre DAWs. Sob estresse CPU-bound, o FL~Studio com
ASIO se manteve estável até $256$~\textit{samples} ($6$~ms), confirmando
o benefício do acesso direto ao hardware: como o ASIO não passa pelo
KMixer, reduz o número de interrupções por ciclo de \textit{buffer}
\cite{microsoft2023asio}. Já o Ableton~Live com MME/DirectX começou a
apresentar \textit{dropouts} em $1024$~\textit{samples}, quatro vezes
acima do ponto de ruptura do FL~Studio, em parte por causa do caminho
pelo KMixer \cite{microsoft2023asio,kim2003}. Vale notar que essa
comparação reflete mais a diferença entre os drivers do que entre os
motores de áudio em si; num cenário em que as duas DAWs usassem ASIO, o
resultado provavelmente seria diferente.



% ----------------------------------------------------------------------------------------------------- %
\chapter{Conclusão}

Este estudo mostrou que, do ponto de vista do gerenciamento de recursos
do SO, o Ableton~Live e o FL~Studio seguem caminhos arquiteturais
opostos. O Ableton~Live aposta na alocação antecipada
(\textit{eager allocation}), reservando memória e criando um
\textit{pool} grande de \textit{threads} desde o início, o que garante
baixa latência de despacho. Já o FL~Studio adota alocação sob demanda
(\textit{lazy allocation}) e tenta compensar a menor quantidade de
\textit{threads} com prioridade de escalonamento mais alta, embora boa
parte desse esforço acabe sendo consumido pela renderização da interface
(\texttt{QuickFontCache}), e não pelo motor de áudio. Em ambiente
controlado, as duas foram classificadas como aptas pelo LatencyMon, com
latências bem abaixo do limite de $1000\,\mu$s, e o uso de drivers
nativos (especialmente ASIO) mostrou-se importante para reduzir a
sobrecarga de CPU. Em ambas, o impacto de processos externos como o
Windows Defender em surto de varredura chegou a ser maior do que as
próprias diferenças entre elas.


% ----------------------------------------------------------------------------------------------------- %
\bibliography{referencias}

\end{document}