% ----------------------------------------------------------------------------------------------------- %
% Arquivo Principal - Trabalho de Sistemas Operacionais
% ----------------------------------------------------------------------------------------------------- %

\documentclass[tcc1]{classe_uftex/uftex}
\newcommand\uftex{UF\TeX}

\usepackage{lipsum}
\usepackage{tikz}
\usepackage[siunitx]{circuitikz}
\usetikzlibrary{arrows}
\usepackage{multirow}
\usepackage{graphicx}
\graphicspath{{images/}}
\usepackage{booktabs}

\usepackage[alf,abnt-emphasize=bf]{referencias_abnt/abntex2cite}
\renewcommand{\backrefpagesname}{}
\renewcommand{\backref}{}
\renewcommand*{\backrefalt}[4]{}

\makelosymbols
\makeloabbreviations

% ========================================================================= %
% PATCH DE CORREÇÃO
% ========================================================================= %
\makeatletter
\renewcommand\maketitle{%
  \ltx@ifpackageloaded{hyperref}{\uftex@hypersetup}{}%
 \begin{titlepage}
 \begin{center}
 \includegraphics[scale=.3]{classe_uftex/logouft}\\
 \setstretch{1}
 \MakeUppercase{\bf\frontcover@maintext}
 \end{center}
  \begin{center}
  \vspace{4cm}
  \nohyphens{\MakeUppercase{\bf\@authname\ \@authsurn}}\par
  \vspace{4cm}
  \nohyphens{\bf\MakeUppercase\local@title}\par
  \vfill
  {\bf\frontpage@bottomtext}
  \end{center}
  \end{titlepage}
  \global\let\maketitle\relax \global\let\and\relax
}

\renewcommand\frontpage@maintext{
  \sloppy\nohyphens{\local@doctype\ apresentado à \local@universityname\ para obten{\c c}{\~ a}o do t{\' i}tulo de \local@degname\ em \local@deptname .}
 \vspace{.5cm}
    \count1=0
    \@whilenum \count1<\@advisor \do{%
    \ifcase\count1
        \nohyphens{\local@advisorstring : \csname uftAdvisorDegree:\the\count1 \endcsname \ \csname uftAdvisorName:\the\count1 \endcsname \ \csname uftAdvisorSurname:\the\count1 \endcsname} \par
    \else
        \nohyphens{\local@coadvisorstring : \csname uftAdvisorDegree:\the\count1 \endcsname \ \csname uftAdvisorName:\the\count1 \endcsname \ \csname uftAdvisorSurname:\the\count1 \endcsname} \par
    \fi
    \advance\count1 by 1}
}

\renewenvironment{abstract}{%
\cleardoublepage \thispagestyle{empty} \chapter*{Resumo} \setlength{\parindent}{0pt}
}{\vspace{.5cm}
\count1=0
\noindent\nohyphens{{\bfseries Palavra-chave}:} \@whilenum \count1<\value{keywords} \do {%
        \nohyphens{\csname uftKeyword:\the\count1 \endcsname.}
        \advance\count1 by 1}\par}

\renewenvironment{foreignabstract}{%
\cleardoublepage \thispagestyle{empty} \chapter*{Abstract} \setlength{\parindent}{0pt}
}{\vspace{.5cm}
\noindent\nohyphens{{\bfseries Keywords}:}
\count1=0
    \@whilenum \count1<\@foreignkeyword \do{%
    \nohyphens{\protect\csname uftForeignkeyword:\the\count1\expandafter\endcsname .}
    \advance\count1 by 1}\par}

\def\@degreename{Bacharel}
\def\local@degname{Bacharel}
\def\local@deptname{Ciência da Computação}
\def\local@programname{Ciência da Computação}
\def\local@doctype{Relatório de Sistemas Operacionais}
\makeatother
% ========================================================================= %

\begin{document}

  \title{Análise Comparativa de Gerenciamento de Recursos do SO: Ableton Live vs. FL Studio}
  \author{Henrique}{Noronha Fernandes}
  \advisor{Prof.}{[Nome do Professor de SO]}{[Sobrenome]}{[Titulação]}

  \department{Ciência da Computação}
  \date{15}{04}{2026}

  \keyword{Sistemas Operacionais}
  \keyword{Escalonamento de Threads}
  \keyword{Gerenciamento de Memória}
  \keyword{Latência de Áudio}
  \keyword{Análise de Desempenho}

  \foreignkeyword{Operating Systems}
  \foreignkeyword{Thread Scheduling}
  \foreignkeyword{Memory Management}
  \foreignkeyword{Audio Latency}
  \foreignkeyword{Performance Analysis}

  \maketitle
  \frontmatter

  \printlosymbols
  \printloabbreviations
  \tableofcontents

\mainmatter
\onehalfspacing

% ----------------------------------------------------------------------------------------------------- %
\chapter{Introdução}

Historicamente, os Sistemas Operacionais (SO) evoluíram de simples monitores
de execução sequencial para ecossistemas complexos de gerenciamento de hardware
e software. Enquanto as primeiras gerações de sistemas focavam estritamente na
abstração de comandos básicos e organização de arquivos, os sistemas
contemporâneos precisam orquestrar uma arquitetura de múltiplos núcleos
(\textit{multicore}), memória virtual e interrupções em milissegundos.
No cenário contemporâneo, os computadores pessoais atuam como Estações de
Trabalho de Áudio Digital (DAWs --- \textit{Digital Audio Workstations}),
executando softwares complexos que exigem do SO uma orquestração extremamente
eficiente dos componentes de hardware. Nesse contexto, a performance de uma
DAW não depende apenas da força bruta do processador, mas de como o sistema
operacional gerencia as \textit{threads} de execução, aloca a memória e lida
com operações de entrada e saída (I/O) em alta velocidade.

O grande desafio imposto por essas aplicações é a necessidade de operar sob
restrições de tempo real. Conforme a classificação de \citeonline{silberschatz2018},
as DAWs enquadram-se como sistemas de tempo real não crítico
(\textit{soft real-time}). Nesses sistemas, embora o atraso no atendimento de
um processo não cause uma falha fatal no computador, ele resulta em uma severa
degradação da qualidade do serviço --- nesse caso, manifestando-se como falhas
e artefatos sonoros indesejados. Para evitar essa degradação, o sistema
operacional precisa garantir que as \textit{threads} de áudio tenham
preferência sobre processos comuns, minimizando a latência de despacho e a
latência de interrupção.

Diante dessa problemática, o presente trabalho realiza uma análise comparativa
do consumo e gerenciamento de recursos do sistema operacional entre duas das
principais DAWs do mercado: Ableton~Live e FL~Studio. Busca-se mapear como
cada arquitetura de software impacta o consumo de CPU, o comportamento
\textit{multithread}, o uso de memória RAM e a latência de I/O sob as mesmas
condições de estresse, demonstrando na prática os impactos das decisões de
escalonamento e arquitetura do SO em ambientes de alta exigência em tempo real.


% ----------------------------------------------------------------------------------------------------- %
\chapter{Referencial Teórico}

\section{Gerenciamento de Processos e \textit{Threads}}

Um \textbf{processo} é um programa em execução, dotado de espaço de
endereçamento próprio e dos recursos a ele atribuídos pelo SO
\cite{silberschatz2018}. Dentro de um processo podem coexistir múltiplas
\textbf{\textit{threads}}: unidades menores de execução que compartilham o
mesmo espaço de endereçamento, mas mantêm pilha e registradores próprios
\cite{tanenbaum2016}. Essa partilha torna a troca de contexto entre
\textit{threads} muito mais barata do que entre processos --- razão pela qual
motores de áudio recorrem ao \textit{multithreading} para distribuir o
processamento de blocos de amostras entre os núcleos do hardware.

\subsection{Escalonador do Windows NT}

O escalonador do Windows NT é \textbf{preemptivo baseado em prioridades},
com 32 níveis (0--31): a faixa de usuário ocupa os níveis 1--15 e a faixa de
tempo real, 16--31 \cite{stallings2017os}. Os valores observados neste
trabalho situam-se na faixa de usuário: \textit{Normal} (8), \textit{Above
Normal} (10) e \textit{High} (12--13). A cada \textit{thread} o sistema
atribui uma \textbf{prioridade base} (derivada da classe do processo) e, em
tempo de execução, pode aplicar um \textbf{\textit{priority boosting}}
temporário --- elevação automática para compensar \textit{starvation} ou
premiar \textit{threads} recém-acordadas de I/O \cite{stallings2017os}. O
\textit{boost} decai gradualmente até retornar à base, o que explica a
divergência entre prioridade base e dinâmica observável em ferramentas como
o \textit{Process Explorer}.

\subsection{Tempo Real Crítico vs.\ Não Crítico}

\citeonline{silberschatz2018} distinguem dois regimes de tempo real. Em
sistemas de \textbf{tempo real crítico} (\textit{hard real-time}), perder um
prazo é uma falha absoluta --- inaceitável em aviônica ou em dispositivos
médicos. Em sistemas de \textbf{tempo real não crítico} (\textit{soft
real-time}), a perda eventual de um prazo degrada a qualidade do serviço,
mas não compromete o sistema. DAWs caem nesta segunda categoria: um
\textit{buffer} não preenchido a tempo produz \textit{dropout} audível, mas
a máquina continua estável. Basta, portanto, que as \textit{threads} de
áudio operem em prioridade superior à dos processos comuns para que o
escalonador as despache primeiro sob contenção.


\section{Interrupções e Latência de I/O em Tempo Real}

Uma \textbf{interrupção de hardware} é um sinal assíncrono pelo qual um
periférico avisa o processador de que requer atenção imediata
\cite{silberschatz2018}. Ao recebê-lo, o processador suspende a
\textit{thread} corrente, salva seu estado e transfere o controle para a
\textbf{Rotina de Serviço de Interrupção} (ISR). O intervalo entre o sinal e
o início da ISR é a \textbf{latência de interrupção}; somada ao tempo de
troca de contexto até a \textit{thread} destino, resulta na
\textbf{latência de despacho} \cite{stallings2017os}. Em áudio, qualquer
atraso de despacho que exceda o período do \textit{buffer} se manifesta como
silêncio ou artefato sonoro.

\subsection{\textit{Callback} de Áudio e Tamanho de \textit{Buffer}}

Drivers de áudio operam no modelo \textbf{\textit{pull callback}}: o hardware
solicita periodicamente um bloco de $N$ amostras e o motor de áudio tem até
o prazo do próximo bloco para entregá-lo \cite{fons2010}. A relação entre
\textit{buffer} e latência é direta:

\begin{equation}
  L = \frac{N}{f_s}
\end{equation}

\noindent onde $L$ é a latência em segundos, $N$ o tamanho do bloco em
amostras e $f_s$ a taxa de amostragem. A 44.100~Hz, $N = 256$ corresponde a
${\approx}5{,}8$~ms. Reduzir $N$ diminui a latência perceptível, mas
multiplica a frequência das interrupções --- e, com ela, a pressão sobre o
escalonador \cite{kim2003}.

\subsection{ASIO vs.\ WDM/DirectSound}

O Windows expõe duas rotas de áudio ao aplicativo. Pelo \textbf{WDM/DirectSound},
o fluxo passa pelo \textit{Kernel Audio Mixer} (KMixer), responsável por
misturar áudio de múltiplos aplicativos antes de enviá-lo ao hardware
\cite{microsoft2023asio}. Cada ciclo de \textit{buffer} acorda o KMixer,
acrescentando latência e interrupções extras. Já o protocolo
\textbf{ASIO} (\textit{Audio Stream Input/Output}) entrega ao driver do
fabricante acesso direto ao hardware, ignorando o KMixer
\cite{microsoft2023asio}. O ganho é uma redução substancial de latência e de
carga sobre o escalonador.

\subsection{Por que DAWs não usam Escalonamento de Prazos}

\citeonline{liu1973} formalizaram o escalonamento em tempo real com o
algoritmo \textit{Rate Monotonic}, e \citeonline{buttazzo2011} mostraram que
a garantia formal de prazos exige análise de pior caso (WCET). DAWs, como
sistemas \textit{soft real-time}, dispensam esse rigor: limitam-se a elevar
a prioridade das \textit{threads} de áudio no escalonador genérico do SO,
trocando garantia formal por simplicidade de implementação.


\section{Gerenciamento de Memória e Paginação}

Cada processo recebe do SO um espaço de endereçamento virtual contíguo e
isolado, independente da memória física disponível \cite{silberschatz2018}.
A tradução entre endereço virtual e físico é feita pela MMU com base nas
tabelas de páginas do \textit{kernel}. Na \textbf{paginação por demanda},
uma página só é mapeada a um quadro físico quando acessada pela primeira
vez --- evento chamado de \textbf{falta de página} (\textit{page fault})
\cite{tanenbaum2016}. Resolvê-la exige suspender a \textit{thread},
localizar (ou ler do disco) a página e retomar a execução: uma latência
não determinística que pode variar de microssegundos a milissegundos.

Para áudio em tempo real, uma falta de página durante o processamento de um
bloco é especialmente cara: a \textit{thread} fica bloqueada enquanto o
prazo do \textit{buffer} avança \cite{kim2003}. Sistemas críticos fixam
páginas na RAM via \texttt{mlock()} (Linux) ou \texttt{VirtualLock()}
(Windows); DAWs comerciais costumam optar por alocação antecipada
(\textit{eager allocation}) como equivalente prático.

\subsection{\textit{Eager} vs.\ \textit{Lazy Allocation}}

\citeonline{tanenbaum2016} contrastam dois modelos de alocação:

\begin{itemize}
    \item \textbf{\textit{Eager allocation}}: reserva e inicializa toda a
    memória prevista logo na inicialização do processo. Elimina faltas de
    página no caminho crítico, ao custo de uma pegada elevada em repouso.

    \item \textbf{\textit{Lazy allocation}}: aloca recursos só quando
    demandados. Mantém pegada mínima em repouso, mas introduz latência
    não determinística no momento da primeira alocação.
\end{itemize}

\subsection{\textit{Private Bytes}}

O contador \textit{Private Bytes} do Windows mede a memória virtual
comprometida exclusivamente pelo processo --- ignorando DLLs e demais
regiões compartilhadas \cite{stallings2018}. É a métrica mais fiel ao custo
real de memória da aplicação, por refletir o que precisaria ser paginado
caso o sistema ficasse sem RAM física. Em DAWs, o crescimento desse contador
ao carregar trilhas reflete sobretudo os \textit{buffers} de pré-leitura e
as estruturas de decodificação mantidas em memória para reprodução de
baixa latência.


% ----------------------------------------------------------------------------------------------------- %
\chapter{Metodologia}

O presente estudo adota uma abordagem quantitativa e comparativa para analisar
o gerenciamento de recursos e o desempenho de \textit{Digital Audio Workstations}
(DAWs) sob a ótica de Sistemas Operacionais. O foco reside na interação entre
o \textit{kernel} do Windows e os motores de áudio do \textbf{Ableton Live 12}
e \textbf{FL Studio 24}.

\section{Ambiente de Teste e Isolamento de Variáveis}

Para garantir a reprodutibilidade, o ambiente foi configurado para minimizar
a interferência de processos em segundo plano. Os testes foram executados em
um notebook \textbf{ASUS Vivobook K3605ZF} com as seguintes especificações:

\begin{itemize}
    \item \textbf{Processador:} Intel Core i5-12500H (12ª geração) ---
    12~núcleos (4 núcleos de desempenho + 8 núcleos de eficiência),
    16~\textit{threads}, frequência base de 3,1~GHz e \textit{boost} de até
    4,5~GHz.
    \item \textbf{Memória RAM:} 16~GB DDR4.
    \item \textbf{GPU:} NVIDIA (driver \texttt{nvlddmkm.sys} v581.29),
    relevante para a análise de DPCs do Teste~4.
    \item \textbf{Sistema Operacional:} Windows 11 (build 26200, x64).
    \item \textbf{Camada de Abstração de Áudio:} Cada DAW foi testada com seu
    driver de áudio nativo. O FL~Studio utilizou o \textit{FL Studio ASIO};
    o Ableton~Live utilizou seu driver nativo padrão (DirectSound/MME).
    \item \textbf{Configuração de saída:} O \textit{Fruity Limiter} nativo do
    FL~Studio foi removido do canal Master antes dos testes, garantindo
    paridade com a arquitetura aditiva do Ableton~Live.
    \item \textbf{Antivírus:} O Windows Defender permaneceu ativo em todos os
    testes, em condições idênticas para ambas as DAWs.
\end{itemize}

\section{Instrumentação e Coleta de Métricas}

\begin{itemize}
    \item \textbf{Performance Monitor (PerfMon):} Ferramenta nativa do Windows
    utilizada para capturar contadores do \textit{kernel} a 1~Hz, exportados
    via \texttt{relog} para formato \texttt{.csv} \cite{microsoft2023perfmon}.
    Métricas coletadas:
    \begin{itemize}
        \item \texttt{\% Tempo de Processador} (\texttt{\_Total})
        \item \texttt{Bytes Particulares (Private Bytes)} por processo
        \item \texttt{Contagem de Threads} por processo
    \end{itemize}
    \item \textbf{Process Explorer (Sysinternals):} Inspeção detalhada de
    \textit{threads} individuais, prioridades de escalonamento e consumo de
    CPU por \textit{thread}.
    \item \textbf{LatencyMon v7.31 (Resplendence):} Medição de latência de
    rotinas DPC e ISR, latência \textit{interrupt-to-process} e contagem de
    \textit{hard pagefaults} em nível de sistema.
\end{itemize}

\section{Protocolo de Teste 1: Consumo em Repouso (\textit{Idle})}

DAW aberta sem projeto carregado. Coleta de 2,5 minutos a 1~Hz via PerfMon.

\section{Protocolo de Teste 2: Carga \textit{I/O-bound} com 20 Trilhas AIFF}

Reprodução simultânea em \textit{loop} de 20 trilhas de áudio no formato
\textbf{AIFF} (PCM não comprimido), evitando ciclos de CPU destinados à
descompressão. Coleta de 3 minutos a 1~Hz.

\section{Protocolo de Teste 3: Escalonamento de \textit{Threads}}

Inspeção via \textit{Process Explorer} com o projeto de 20~trilhas em
reprodução. Registraram-se: total de \textit{threads} ativas, prioridade base
e dinâmica da \textit{thread} principal, módulos de origem das
\textit{threads} e CPU consumida por \textit{thread}.

\section{Protocolo de Teste 4: Latência com Driver Nativo (LatencyMon)}

Medição da latência de driver com o \textbf{LatencyMon v7.31}, executado
durante 2 minutos e 50 segundos com o projeto de 20 trilhas AIFF em
reprodução em cada DAW, utilizando os drivers nativos sem alteração de
configurações de \textit{buffer}. O Windows Defender permaneceu ativo em
ambos os testes, garantindo paridade de condições. Métricas coletadas:
latência máxima e média \textit{interrupt-to-process}, tempo máximo de
rotinas DPC e ISR, percentual de CPU em DPCs/ISRs e contagem total de
\textit{hard pagefaults}.

\section{Protocolo de Teste 5: Estresse CPU-bound e Ponto de Ruptura}

Introdução de carga de processamento intensivo via 5~instâncias do plugin
de reverb nativo de cada DAW inseridas no canal Master, com configuração
padrão de fábrica. Com as 20~trilhas AIFF em reprodução simultânea e os
reverbs ativos, o \textit{buffer size} foi reduzido progressivamente
(4096 $\rightarrow$ 2048 $\rightarrow$ 1024 $\rightarrow$ 512 $\rightarrow$
256 $\rightarrow$ 128~\textit{samples}), aguardando 20~segundos em cada
etapa. O ponto de ruptura (\textit{buffer underrun}) foi identificado pela
ocorrência de \textit{clicks}, \textit{pops} ou cortes no sinal de áudio.
O FL~Studio utilizou o driver \textbf{FL Studio ASIO}; o Ableton~Live
utilizou \textbf{MME/DirectX}, preservando as escolhas nativas de cada
fabricante.


% ----------------------------------------------------------------------------------------------------- %
\chapter{Resultados}

\section{Teste 1 --- Consumo em Repouso (\textit{Idle})}

O Teste~1 estabeleceu o consumo de recursos de ambas as DAWs sem nenhum
projeto carregado. Os dados foram coletados durante aproximadamente 2,5~minutos,
resultando em 146 amostras válidas para o Ableton~Live~12 e 145 para o
FL~Studio~24.

\begin{table}[ht]
\centering
\caption{Resumo estatístico --- Teste~1: consumo em repouso}
\label{tab:t1}
\begin{tabular}{llccc}
\toprule
\textbf{Métrica} & \textbf{DAW} & \textbf{Mínimo} & \textbf{Média} & \textbf{Máximo} \\
\midrule
\multirow{2}{*}{\% CPU}
  & Ableton Live 12  & 0{,}00\% & 10{,}95\% & 33{,}73\% \\
  & FL Studio (FL64) & 0{,}00\% & 12{,}38\% & 37{,}72\% \\
\midrule
\multirow{2}{*}{Private Bytes (MB)}
  & Ableton Live 12  & 338{,}6 & 536{,}8 & 538{,}3 \\
  & FL Studio (FL64) &  95{,}6 &  95{,}8 &  95{,}8 \\
\midrule
\multirow{2}{*}{Threads ativas}
  & Ableton Live 12  &  8 & 54{,}8 & 56 \\
  & FL Studio (FL64) & 44 & 44{,}0 & 45 \\
\bottomrule
\multicolumn{5}{l}{\footnotesize Fonte: dados coletados pelos autores via PerfMon, 26 maio 2026.}
\end{tabular}
\end{table}

\subsection{CPU em repouso}

O FL~Studio registrou média de $12{,}38\%$, ligeiramente superior ao
Ableton~Live ($10{,}95\%$), com pico máximo de $37{,}72\%$ frente a $33{,}73\%$.
A diferença é sistemática ao longo de toda a sessão (Figura~\ref{fig:t1cpu}),
evidenciando processos internos contínuos mesmo sem projeto ativo.

\begin{figure}[ht]
\centering
\includegraphics[width=0.95\textwidth]{grafico_cpu}
\caption{Consumo de CPU em repouso --- Teste~1.}
\label{fig:t1cpu}
\par\footnotesize{Fonte: dados coletados pelos autores via PerfMon, 26 maio 2026.}
\end{figure}

\subsection{RAM em repouso}

O Ableton~Live alocou em média $536{,}8$~MB em repouso, valor $5{,}6\times$
superior ao FL~Studio ($95{,}8$~MB), que manteve consumo praticamente constante
(variação $< 0{,}2$~MB) (Figura~\ref{fig:t1ram}).

\begin{figure}[ht]
\centering
\includegraphics[width=0.65\textwidth]{grafico_ram}
\caption{Uso de RAM (\textit{Private Bytes}) em repouso --- Teste~1.}
\label{fig:t1ram}
\par\footnotesize{Fonte: dados coletados pelos autores via PerfMon, 26 maio 2026.}
\end{figure}

\subsection{\textit{Threads} em repouso}

O Ableton~Live manteve em média $54{,}8$~\textit{threads} ativas em repouso,
contra $44$~\textit{threads} estáveis do FL~Studio (Figura~\ref{fig:t1thr}).

\begin{figure}[ht]
\centering
\includegraphics[width=0.65\textwidth]{grafico_threads}
\caption{Contagem de \textit{threads} em repouso --- Teste~1.}
\label{fig:t1thr}
\par\footnotesize{Fonte: dados coletados pelos autores via PerfMon, 26 maio 2026.}
\end{figure}

% ---------------------------------------------------------------- %
\section{Teste 2 --- Carga \textit{I/O-bound}: 20 Trilhas AIFF}

O Teste~2 introduziu carga de trabalho real com a reprodução simultânea em
\textit{loop} de 20 trilhas de áudio AIFF (PCM não comprimido). Foram
coletadas 178 amostras para o Ableton~Live~12 e 169 para o FL~Studio~24,
durante aproximadamente 3~minutos cada.

\begin{table}[ht]
\centering
\caption{Resumo estatístico --- Teste~2: 20 trilhas AIFF em reprodução}
\label{tab:t2}
\begin{tabular}{llccc}
\toprule
\textbf{Métrica} & \textbf{DAW} & \textbf{Mínimo} & \textbf{Média} & \textbf{Máximo} \\
\midrule
\multirow{2}{*}{\% CPU}
  & Ableton Live 12  & 0{,}00\% &  3{,}74\% & 20{,}11\% \\
  & FL Studio (FL64) & 0{,}00\% &  4{,}80\% & 31{,}16\% \\
\midrule
\multirow{2}{*}{Private Bytes (MB)}
  & Ableton Live 12  & 601{,}6 & 601{,}7 & 604{,}5 \\
  & FL Studio (FL64) & 137{,}6 & 147{,}6 & 147{,}6 \\
\midrule
\multirow{2}{*}{Threads ativas}
  & Ableton Live 12  & 54 & 55{,}3 & 56 \\
  & FL Studio (FL64) & 26 & 27{,}0 & 28 \\
\bottomrule
\multicolumn{5}{l}{\footnotesize Fonte: dados coletados pelos autores via PerfMon, 26 maio 2026.}
\end{tabular}
\end{table}

\subsection{CPU com 20 trilhas}

Com a carga \textit{I/O-bound} ativa, ambas as DAWs apresentaram redução
expressiva no consumo médio de CPU em relação ao repouso: o Ableton~Live caiu
de $10{,}95\%$ para $3{,}74\%$ ($-6{,}21$~pp) e o FL~Studio de $12{,}38\%$
para $4{,}80\%$ ($-7{,}58$~pp). A Figura~\ref{fig:t2cpu} ilustra o
comportamento ao longo do teste, e a Figura~\ref{fig:t2cpucomp} compara os
dois cenários.

\begin{figure}[ht]
\centering
\includegraphics[width=0.95\textwidth]{t2_grafico_cpu}
\caption{Consumo de CPU durante reprodução de 20 trilhas AIFF --- Teste~2.}
\label{fig:t2cpu}
\par\footnotesize{Fonte: dados coletados pelos autores via PerfMon, 26 maio 2026.}
\end{figure}

\begin{figure}[ht]
\centering
\includegraphics[width=0.70\textwidth]{t2_cpu_comparativo}
\caption{CPU média comparativa: repouso (Teste~1) vs.\ 20 trilhas (Teste~2).}
\label{fig:t2cpucomp}
\par\footnotesize{Fonte: dados coletados pelos autores via PerfMon, 26 maio 2026.}
\end{figure}

\subsection{RAM com 20 trilhas}

O Ableton~Live elevou seu consumo de $536{,}8$~MB para $601{,}7$~MB
($+64{,}9$~MB). O FL~Studio partiu de $95{,}8$~MB e estabilizou em
$147{,}6$~MB ($+51{,}8$~MB). O Ableton consumiu $4{,}1\times$ mais RAM que
o FL~Studio com 20 trilhas ativas (Figura~\ref{fig:t2ram}).

\begin{figure}[ht]
\centering
\includegraphics[width=0.70\textwidth]{t2_ram_comparativo}
\caption{RAM média comparativa: repouso (Teste~1) vs.\ 20 trilhas (Teste~2).}
\label{fig:t2ram}
\par\footnotesize{Fonte: dados coletados pelos autores via PerfMon, 26 maio 2026.}
\end{figure}

\subsection{\textit{Threads} com 20 trilhas}

O FL~Studio reduziu sua contagem de $44$ para $27$~\textit{threads} ao
carregar o projeto ($-38\%$), enquanto o Ableton~Live manteve-se estável
($54{,}8 \rightarrow 55{,}3$) (Figura~\ref{fig:t2thr}).

\begin{figure}[ht]
\centering
\includegraphics[width=0.65\textwidth]{t2_grafico_threads}
\caption{Contagem de \textit{threads} com 20 trilhas ativas --- Teste~2.}
\label{fig:t2thr}
\par\footnotesize{Fonte: dados coletados pelos autores via PerfMon, 26 maio 2026.}
\end{figure}

% ---------------------------------------------------------------- %
\section{Teste 3 --- Escalonamento de \textit{Threads} (Process Explorer)}

O Teste~3 inspecionou a composição interna de \textit{threads} de cada DAW
com o projeto de 20 trilhas AIFF em reprodução ativa via \textit{Process
Explorer}. A Tabela~\ref{tab:t3} resume os dados coletados.

\begin{table}[ht]
\centering
\caption{Escalonamento de \textit{threads} --- Teste~3 (Process Explorer)}
\label{tab:t3}
\begin{tabular}{lcc}
\toprule
\textbf{Parâmetro} & \textbf{FL Studio (FL64)} & \textbf{Ableton Live 12} \\
\midrule
Total de \textit{threads}          & 29                & 55                \\
Prioridade base (thread principal) & 10 (Above Normal) & 8 (Normal)        \\
Prioridade dinâmica                & 12 (High)         & 10 (Above Normal) \\
CPU da thread principal            & 0{,}93\%          & 0{,}75\%          \\
Thread \textit{Time Critical} (15)?& Não               & Não               \\
Módulo dominante                   & QuickFontCache    & ucrtbase          \\
Thread de driver de áudio          & ilwasapi2asio\_x64& DSOUND.dll        \\
\bottomrule
\multicolumn{3}{l}{\footnotesize Fonte: dados coletados pelos autores via Process Explorer, 26 maio 2026.}
\end{tabular}
\end{table}

\subsection{Prioridade de escalonamento}

O FL~Studio agenda sua \textit{thread} principal com prioridade base~10
(\textit{Above Normal}) e dinâmica~12 (\textit{High}), enquanto o
Ableton~Live utiliza base~8 (\textit{Normal}) e dinâmica~10. Nenhuma das
duas DAWs atingiu o nível \textit{Time Critical} (15)
(Figura~\ref{fig:t3prio}).

\begin{figure}[ht]
\centering
\includegraphics[width=0.80\textwidth]{t3_prioridades}
\caption{Prioridades de escalonamento da \textit{thread} principal --- Teste~3.}
\label{fig:t3prio}
\par\footnotesize{Fonte: dados coletados pelos autores via Process Explorer, 26 maio 2026.}
\end{figure}

\subsection{Composição de \textit{threads} por módulo}

No FL~Studio, 14 das 29~\textit{threads} pertencem ao \texttt{QuickFontCache}
(renderização de fontes da UI) e apenas 5 ao \texttt{FLEngine\_x64} (motor de
áudio). No Ableton~Live, ${\approx}30$ das 55~\textit{threads} provêm do
\texttt{ucrtbase.dll} (\textit{thread pool} do C runtime), com o driver de
áudio representado por \texttt{DSOUND.dll} (Figura~\ref{fig:t3comp}).

\begin{figure}[ht]
\centering
\includegraphics[width=0.95\textwidth]{t3_composicao_threads}
\caption{Composição de \textit{threads} por módulo --- Teste~3.}
\label{fig:t3comp}
\par\footnotesize{Fonte: dados coletados pelos autores via Process Explorer, 26 maio 2026.}
\end{figure}

% ---------------------------------------------------------------- %
\section{Teste 4 --- Latência com Driver Nativo (LatencyMon)}

O Teste~4 mediu a latência de \textit{interrupt-to-process} e o comportamento
de rotinas DPC e ISR durante a reprodução das 20 trilhas AIFF, com duração de
2 minutos e 50 segundos por DAW. A Tabela~\ref{tab:t4} apresenta o resumo.

\begin{table}[ht]
\centering
\caption{Resultados de latência --- Teste~4 (LatencyMon v7.31)}
\label{tab:t4}
\begin{tabular}{lcc}
\toprule
\textbf{Métrica} & \textbf{Ableton Live 12} & \textbf{FL Studio (FL64)} \\
\midrule
Conclusão do sistema              & \textbf{Apto}     & \textbf{Não apto} \\
Latência máx. int.-to-proc. ($\mu$s) & 805{,}40       & 5274{,}90         \\
Latência méd. int.-to-proc. ($\mu$s) &    8{,}50      &   14{,}68         \\
DPC máximo ($\mu$s)               & 449{,}81          & 551{,}84          \\
ISR máximo ($\mu$s)               & 267{,}52          & 267{,}35          \\
Tempo total em DPCs (\%)          & 0{,}074           & 0{,}111           \\
Tempo total em ISRs (\%)          & 0{,}023           & 0{,}045           \\
\textit{Hard pagefaults} totais   & 306               & 380               \\
Driver com maior DPC              & \texttt{tcpip.sys}& \texttt{nvlddmkm.sys} \\
Driver com maior ISR              & \multicolumn{2}{c}{\texttt{Wdf01000.sys}} \\
\bottomrule
\multicolumn{3}{l}{\footnotesize Fonte: dados coletados pelos autores via LatencyMon v7.31, 26 maio 2026.}
\end{tabular}
\end{table}

\subsection{Latência \textit{interrupt-to-process}}

O Ableton~Live registrou pico de $805{,}40\,\mu$s --- dentro do limiar
aceitável para áudio em tempo real. O FL~Studio atingiu $5274{,}90\,\mu$s
($6{,}5\times$ maior), levando o LatencyMon a classificar o sistema como
\textit{não apto}, com risco de \textit{buffer underruns}
(Figura~\ref{fig:t4lat}). O driver responsável pelo maior DPC no FL~Studio
foi o \texttt{nvlddmkm.sys} (NVIDIA, $551{,}84\,\mu$s), e o processo com
maior contagem de \textit{hard pagefaults} foi o \texttt{msmpeng.exe}
(Windows Defender) em ambos os testes.

\begin{figure}[ht]
\centering
\includegraphics[width=0.48\textwidth]{LatencyMonMainLive}
\hfill
\includegraphics[width=0.48\textwidth]{LatencyMonMainFL}
\caption{Tela principal do LatencyMon --- Ableton~Live~12 (esquerda, conclusão
         verde: apto) e FL~Studio (direita, conclusão vermelha: não apto).
         Latência máxima \textit{interrupt-to-process}: $805\,\mu$s vs.\
         $5274\,\mu$s.}
\label{fig:t4lat}
\par\footnotesize{Fonte: dados coletados pelos autores via LatencyMon v7.31, 26 maio 2026.}
\end{figure}

\subsection{\textit{Hard pagefaults} e tempo de CPU em DPCs/ISRs}

O FL~Studio registrou maior contagem de \textit{hard pagefaults} (380 vs.\
306) e maior percentual de CPU em DPCs ($0{,}111\%$ vs.\ $0{,}074\%$) e
ISRs ($0{,}045\%$ vs.\ $0{,}023\%$) (Figura~\ref{fig:t4pf}).

\begin{figure}[ht]
\centering
\includegraphics[width=0.48\textwidth]{LatencyMonStatsLive}
\hfill
\includegraphics[width=0.48\textwidth]{LatencyMonStatsFL}
\caption{Relatório detalhado do LatencyMon --- Ableton~Live~12 (esquerda)
         e FL~Studio (direita). Detalhe para os \textit{hard pagefaults}
         (306 vs.\ 380) e o driver \texttt{nvlddmkm.sys} (NVIDIA) como
         principal responsável pelo DPC elevado no FL~Studio.}
\label{fig:t4pf}
\par\footnotesize{Fonte: dados coletados pelos autores via LatencyMon v7.31, 26 maio 2026.}
\end{figure}


% ---------------------------------------------------------------- %
\section{Teste 5 --- Estresse CPU-bound e Ponto de Ruptura}

O Teste~5 introduziu carga de processamento de ponto flutuante por meio de
5~instâncias do reverb nativo de cada DAW no canal Master, combinadas à
reprodução das 20~trilhas AIFF. O \textit{buffer size} foi reduzido
progressivamente até o surgimento de \textit{dropouts} audíveis. A
Tabela~\ref{tab:t5} sintetiza os resultados.

\begin{table}[ht]
\centering
\caption{Ponto de ruptura --- Teste~5 (estresse CPU-bound)}
\label{tab:t5}
\begin{tabular}{lcccc}
\toprule
\textbf{Buffer (smp)} & \textbf{Latência (ms)} &
\textbf{FL Studio (ASIO)} & \textbf{Ableton (MME/DirectX)} \\
\midrule
4096 & 92{,}9 & ---         & Estável     \\
2048 & 46{,}4 & ---         & Estável     \\
1024 & 23{,}2 & ---         & \textbf{Glitch} \\
 512 & 11{,}6 & Estável     & Glitch piora\\
 384 &  9{,}0 & Estável     & ---         \\
 256 &  5{,}8 & \textbf{Mínimo estável} & Glitch forte\\
 128 &  2{,}9 & \textbf{Glitch}         & ---         \\
\bottomrule
\multicolumn{4}{l}{\footnotesize Fonte: dados coletados pelos autores, 27 maio 2026.}
\end{tabular}
\end{table}

\subsection{Ponto de ruptura por driver}

O FL~Studio, operando com o driver \textbf{FL Studio ASIO}, manteve-se
estável até $256$~\textit{samples} ($6$~ms), apresentando \textit{dropout}
apenas ao atingir $128$~\textit{samples}. O Ableton~Live, utilizando
\textbf{MME/DirectX}, iniciou os cortes já em $1024$~\textit{samples}
($23{,}2$~ms), com degradação progressiva até $256$~\textit{samples}.
A Figura~\ref{fig:t5lat} ilustra a relação entre \textit{buffer size} e
latência ao longo do teste, com os pontos de ruptura assinalados.

\begin{figure}[ht]
\centering
\includegraphics[width=0.95\textwidth]{t5_buffer_latencia}
\caption{Buffer size vs.\ latência ao longo do Teste~5. O marcador
         $\times$ indica o ponto de ocorrência de \textit{dropout} audível
         em cada DAW.}
\label{fig:t5lat}
\par\footnotesize{Fonte: dados coletados pelos autores, 27 maio 2026.}
\end{figure}

\begin{figure}[ht]
\centering
\includegraphics[width=0.75\textwidth]{t5_ruptura_comparativo}
\caption{Comparativo do \textit{buffer} mínimo estável e latência
         correspondente --- Teste~5.}
\label{fig:t5comp}
\par\footnotesize{Fonte: dados coletados pelos autores, 27 maio 2026.}
\end{figure}

\section{Consumo de CPU e Comportamento Multithread}

\section{Análise de Latência e Buffer}


% ----------------------------------------------------------------------------------------------------- %
\chapter{Discussão}

\section{Análise Comparativa --- Teste~1 (Repouso)}

Os resultados do Teste~1 revelam perfis de inicialização distintos. O
\textbf{Ableton~Live} adota \textit{eager allocation}: aloca memória e cria
\textit{threads} antecipadamente (${\approx}537$~MB, 55~\textit{threads}),
garantindo baixa latência de despacho desde a inicialização
\cite{silberschatz2018}. O \textbf{FL~Studio} demonstrou \textit{lazy
allocation}: mantém apenas estruturas mínimas em repouso (${\approx}96$~MB,
44~\textit{threads}), postergando alocações para o carregamento do projeto
\cite{tanenbaum2016}.

\section{Análise Comparativa --- Teste~2 (20 Trilhas AIFF)}

O Teste~2 revelou três achados relevantes. Primeiro, a \textbf{redução de
CPU sob carga \textit{I/O-bound}} em ambas as DAWs é explicada pelo driver
de áudio nativo: ao assumir controle exclusivo do hardware, elimina a
sobrecarga do \textit{Kernel Mixer} do Windows. Segundo, o
\textbf{crescimento proporcional de RAM} foi semelhante em termos absolutos
($+65$~MB no Ableton, $+52$~MB no FL~Studio), indicando custo por trilha
AIFF comparável entre as arquiteturas. Terceiro, a \textbf{queda de
\textit{threads} no FL~Studio} (de 44 para 27) indica consolidação de
\textit{threads} genéricas em especializadas de áudio ao carregar o projeto
\cite{stallings2017os}.

\section{Análise Comparativa --- Teste~3 (Escalonamento de \textit{Threads})}

O \textbf{FL~Studio} opera com prioridade mais elevada (base~10,
dinâmica~12), obtendo maior fatia de CPU em situações de concorrência
\cite{silberschatz2018}. Contudo, 14 das 29~\textit{threads} pertencem ao
\texttt{QuickFontCache}, indicando que parte do orçamento de CPU é consumida
pela interface gráfica, e não pelo motor de áudio. O \textbf{Ableton~Live},
com prioridade base inferior (8), distribui sua carga em 55~\textit{threads}
via \textit{thread pool} do \texttt{ucrtbase}, favorecendo escalabilidade nos
16~\textit{threads} lógicos do i5-12500H \cite{tanenbaum2016}. Nenhuma das
DAWs usa prioridade \textit{Time Critical} (15), confirmando o enquadramento
\textit{soft real-time} \cite{stallings2017os}.

\section{Análise Comparativa --- Teste~4 (Latência)}

O Teste~4 revelou a diferença mais crítica entre as duas DAWs. O
Ableton~Live manteve latência máxima dentro do limiar aceitável
($805\,\mu$s), enquanto o FL~Studio atingiu $5274\,\mu$s --- classificado
como problemático pelo LatencyMon. O driver \texttt{nvlddmkm.sys} (NVIDIA)
foi o principal responsável pelos DPCs elevados durante o teste do
FL~Studio, e o \texttt{msmpeng.exe} (Windows Defender) gerou o maior número
de \textit{hard pagefaults} em ambos os casos \cite{silberschatz2018}. O
fato de o Ableton ter permanecido abaixo do limiar crítico nas mesmas
condições sugere que sua arquitetura de \textit{eager allocation} --- com
memória pré-alocada e mais \textit{threads} prontas --- reduz a frequência
de \textit{hard pagefaults} e, consequentemente, a latência máxima de
resposta \cite{tanenbaum2016}.


\section{Análise Comparativa --- Teste~5 (Estresse CPU-bound)}

O resultado do Teste~5 deve ser interpretado com cautela, pois as duas DAWs
utilizaram drivers de áudio distintos --- diferença que constitui, em si
mesma, um dado relevante sobre as escolhas arquiteturais de cada fabricante.

O \textbf{FL~Studio} com driver \textbf{ASIO} sustentou reprodução estável
até $256$~\textit{samples} ($6$~ms), confirmando o benefício do acesso
direto ao hardware proporcionado pelo protocolo ASIO: ao contornar o
\textit{Kernel Audio Mixer} (KMixer) do Windows, o driver reduz o número de
interrupções por ciclo de \textit{buffer} e diminui a pressão sobre o
escalonador \cite{microsoft2023asio}. Esse resultado é consistente com a
prioridade de \textit{thread} mais elevada (base~10, dinâmica~12) observada
no Teste~3, que garante ao motor de áudio maior janela de tempo para preencher
cada bloco antes do prazo.

O \textbf{Ableton~Live} com \textbf{MME/DirectX} iniciou os \textit{dropouts}
em $1024$~\textit{samples} ($23{,}2$~ms) --- quatro vezes acima do ponto de
ruptura do FL~Studio. Esse comportamento é previsível à luz do referencial
teórico: o caminho WDM/DirectX passa pelo KMixer, que adiciona latência e
interrupções extras a cada ciclo de \textit{buffer}
\cite{microsoft2023asio,kim2003}. Sob carga CPU-bound (5~reverbs em convolução
simultâneos), esse custo adicional torna inviável a manutenção de buffers
menores sem \textit{underruns}.

Ressalta-se que a comparação direta dos pontos de ruptura reflete sobretudo
a diferença entre os modelos de driver, e não necessariamente a capacidade
intrínseca do motor de áudio de cada DAW. Em um cenário com ambas as DAWs
utilizando ASIO, o resultado poderia ser diferente --- hipótese que constitui
uma limitação deste estudo e uma direção para trabalhos futuros.



% ----------------------------------------------------------------------------------------------------- %
\chapter{Conclusão}

O presente estudo demonstrou que, sob a perspectiva do gerenciamento de
recursos do Sistema Operacional, Ableton~Live e FL~Studio seguem abordagens
arquiteturais bem diferentes. Em resumo, os resultados obtidos mostram que
o Ableton~Live prioriza a alocação antecipada (\textit{eager allocation}):
a aplicação retém antecipadamente o espaço necessário e estabelece um
extenso \textit{pool} de \textit{threads} desde o início para garantir
baixa latência de despacho e capacidade de dividir o trabalho de forma
eficiente entre os 16~\textit{threads} lógicos do hardware utilizado. Em
contrapartida, o FL~Studio baseia-se em alocação sob demanda (\textit{lazy
allocation}), operando com estruturas mínimas até que a carga de trabalho
seja exigida.

Verificou-se também que o FL~Studio tenta contornar gargalos ao atribuir
prioridade de escalonamento mais alta (nível dinâmico~12) à sua
\textit{thread} principal. Contudo, a análise revelou que parte significativa
desse esforço computacional não é utilizada pelo motor de áudio, mas sim
consumida pela renderização da interface gráfica (módulo
\texttt{QuickFontCache}). O teste de latência confirmou essa limitação: sob
as mesmas condições de sistema --- incluindo o driver NVIDIA
(\texttt{nvlddmkm.sys}) e o Windows Defender (\texttt{msmpeng.exe}) ativos
--- o Ableton~Live manteve-se dentro do limiar aceitável ($805\,\mu$s),
enquanto o FL~Studio atingiu $5274\,\mu$s, sendo classificado pelo
LatencyMon como \textit{não apto} para áudio em tempo real sem
\textit{dropouts}.

Por fim, constatou-se que o uso de \textit{drivers} nativos mitiga a
sobrecarga de CPU ao eliminar a intervenção do \textit{Kernel Mixer} do
Windows, e que ambas as DAWs operam estritamente como sistemas de tempo
real não crítico (\textit{soft real-time}), sem atingir níveis de prioridade
\textit{Time Critical}.

No que diz respeito às aplicações práticas, os resultados fornecem diretrizes
para dimensionamento de hardware e design de software. Para usuários do
Ableton~Live, estações de trabalho com ampla RAM e processadores com alto
número de núcleos são mais vantajosas. Para o ecossistema do FL~Studio,
processadores com alto desempenho \textit{single-core} podem compensar a
concentração de carga na \textit{thread} principal. Do ponto de vista da
Engenharia de Software, os dados evidenciam a necessidade de isolar de forma
mais eficiente as \textit{threads} de processamento de áudio das rotinas de
interface gráfica, garantindo que o escalonador do SO não penalize a entrega
de dados sensíveis ao tempo em favor da renderização visual.


% ----------------------------------------------------------------------------------------------------- %
\bibliography{referencias}

\end{document}